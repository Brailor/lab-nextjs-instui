module.exports = {
    webpack: (config, {isServer}) => {
        
        // config.cache = false
        // config.devtool = 'eval-cheap-module-source-map'
        // console.log({config})

        return config
    }
}